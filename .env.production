# CoralSend production environment
# Target domain: coralsend.com
# Server public IP: 109.199.112.84
#
# Notes:
# - App domain can stay behind Cloudflare proxy (orange cloud).
# - TURN domain MUST be DNS only (gray cloud), unless you use Cloudflare Spectrum.
# - You manage TLS yourself in production.

# ------------------------------------------------------------------------------
# Web app / signaling
# ------------------------------------------------------------------------------

# Root deployment (empty for domain root)
NEXT_PUBLIC_BASE_PATH=

# Public signaling endpoint used by browser clients
NEXT_PUBLIC_SIGNALING_URL=wss://coralsend.com/ws

# ------------------------------------------------------------------------------
# WebRTC ICE (browser side)
# ------------------------------------------------------------------------------

# STUN endpoint (self-hosted coturn)
NEXT_PUBLIC_STUN_URL=stun:turn.coralsend.com:3478

# TURN endpoint (simplest policy: UDP only)
NEXT_PUBLIC_TURN_URL=turn:turn.coralsend.com:3478?transport=udp

# TURN long-term credentials exposed to browser for ICE auth
# Rotate periodically in production.
NEXT_PUBLIC_TURN_USER=coralsend_prod
NEXT_PUBLIC_TURN_PASS=8zQmL4rV9tK2pH7xN3wF6cD1sB5yU8

# ------------------------------------------------------------------------------
# coturn runtime settings (server side)
# ------------------------------------------------------------------------------

# TURN realm should match your TURN DNS name
TURN_REALM=turn.coralsend.com

# Must match browser TURN credentials above for this static-credential setup
TURN_USER=coralsend_prod
TURN_PASSWORD=8zQmL4rV9tK2pH7xN3wF6cD1sB5yU8

# Public server IP used by coturn for relayed candidates
TURN_EXTERNAL_IP=109.199.112.84

# ------------------------------------------------------------------------------
# Container images (GHCR)
# ------------------------------------------------------------------------------

# Dokploy/Compose will pull these production images.
SERVER_IMAGE=ghcr.io/alifakhimi/coralsend-server:latest
WEB_IMAGE=ghcr.io/alifakhimi/coralsend-web:latest
